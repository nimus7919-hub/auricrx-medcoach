# Render configuration for AuricRX MedCoach API
services:
  - type: web
    name: auricrx-medcoach-api
    env: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
